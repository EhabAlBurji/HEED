Place the 12 yarn/crochet avatar images here with these EXACT filenames:

  notebook.png   — دفتر صفراء       (yellow bg)
  dog.png        — كلب أبيض          (blue bg)
  bulb.png       — مصباح أصفر        (yellow bg)
  house.png      — بيت أزرق          (light blue bg)
  bike.png       — دراجة خضراء       (yellow-green bg)
  carrot.png     — جزرة برتقالية     (light green bg)
  book.png       — كتاب مفتوح        (beige bg)
  coffee.png     — كوب قهوة          (light green bg)
  clock.png      — منبه أحمر         (yellow bg)
  fork.png       — شوكة وملعقة       (yellow bg)
  heart.png      — قلب وردي          (pink bg)
  soccer.png     — كرة قدم           (green bg)

Recommended size: 256×256 PNG (or larger square). Transparent background
optional — the colored circle bg is rendered by CSS as a fallback.

These map to the AvatarOption entries in src/lib/avatars.ts.
